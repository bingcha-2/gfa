// Test stub for Next.js's `server-only` marker package. In a real build it
// throws when imported into a client bundle; under vitest it's a no-op so
// server modules (e.g. user-server-api.ts) can be unit-tested directly.
export {};
