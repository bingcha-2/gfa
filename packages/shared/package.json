{
  "name": "@gfa/shared",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\" && tsc -p tsconfig.json && node -e \"for (const f of ['pricing.json','quota-rates.json','api-pricing.json']) require('fs').copyFileSync('src/'+f,'dist/'+f)\"",
    "dev": "tsc -p tsconfig.json --watch",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test:unit": "vitest run src"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^4.1.1"
  }
}
